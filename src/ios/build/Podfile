workspace '../../platforms/ios/${PROJECT_NAME}'

use_frameworks!

target '${PROJECT_NAME}' do
    project '../../platforms/ios/${PROJECT_NAME}'

    # Dependencies go here

    # Dependencies end

    target 'native' do
        inherit! :search_paths
        project 'native'
    end
end




